import React from "react";
import ReactDOM from "react-dom/client";
import WreathOrderForm from "./WreathOrderForm.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <WreathOrderForm />
  </React.StrictMode>
);
